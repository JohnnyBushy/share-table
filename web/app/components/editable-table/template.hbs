<tbody>
  {{#each @rows as |row index|}}
    <tr class="editable-table__row">
      {{#each row.cells as |cell index|}}
        <td class="editable-table__cell">
          <input
            class="editable-table__cell-input"
            placeholder="Cell"
            value={{cell.value}}
            onchange={{action (mut cell.value) value="target.value"}}
          >
        </td>
      {{/each}}
    </tr>
  {{/each}}
</tbody>